{
  "name": "postgres-container",
  "version": "0.0.1",
  "description": "Postgres container which can be used for integration tests.",
  "main": "src/container.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/ayecue/postgres-container.git"
  },
  "scripts": {
    "test": "mocha 'test/**/*.test.js' --require test/prepare"
  },
  "dependencies": {
    "pg": "^7.0.2",
    "dockerode": "^2.5.0"
  },
  "devDependencies": {
    "mocha": "^3.2.0",
    "mocha-prepare-promise": "^0.0.1",
    "referee": "^1.2.0"
  }
}
